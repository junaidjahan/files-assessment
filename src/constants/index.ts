export const API_ENDPOINTS = {
  ITEMS: '/items.json',
  FAVORITES: '/favorites.json',
} as const;

export const LAYOUT = {
  SIDEBAR_WIDTH: 200,
  SIDEBAR_PADDING: 20,
  CONTENT_PADDING: 30,
  PAPER_MARGIN: 20,
} as const;

export const Z_INDEX = {
  DROPDOWN: 1000,
} as const;

export const VIEW_TABS = {
  GRID: 'grid',
  TABLE: 'table',
} as const;
